# JRuby Azure Site Extension
This site extension will add jruby to Your Azure Webapp.
It is automatically configured:
 * install gems after deployment
 * to start ```rackup``` with proper azure port for IIS server
